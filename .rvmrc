
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.8.7@rubygolf" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.8.7@rubygolf"
else
  rvm --create  "ruby-1.8.7@rubygolf"
fi
